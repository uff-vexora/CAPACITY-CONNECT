import { AuthForm } from "./AuthForm";
export function Signup({ setPage, role, onAuthenticated }) { return <AuthForm page="Signup" setPage={setPage} role={role} onAuthenticated={onAuthenticated} />; }
